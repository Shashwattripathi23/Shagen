import React from "react";
import { Requirements } from "../components/requirements";

export default {
  title: "App Test/Requirements",
  component: Requirements,
};

export const Default = () => <Requirements />;
